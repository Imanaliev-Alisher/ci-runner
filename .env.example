# GitHub repository configuration
# Format: OWNER/REPO (e.g., "myorg/myrepo")
OWNER=your-github-username
REPO=your-repository-name

# GitHub Personal Access Token
# Required scopes: repo, admin:repo_hook
TOKEN=your_github_token_here

# Runner name (will be visible in GitHub Actions settings)
RUNNER_NAME=docker-runner

# Labels for the runner (comma-separated)
# Example: self-hosted,linux,docker
LABELS=self-hosted,linux,docker

# Docker group ID (auto-detected on Linux)
# On Linux, use: $(stat -c '%g' /var/run/docker.sock)
# On Windows/WSL, you may need to set this manually
DOCKER_GID=999